server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:/opt/shiftsync/config-repo,file:./config-repo,file:./shiftsync-platform/config-server/config-repo,classpath:/config-repo